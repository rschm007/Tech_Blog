<footer>
    <ul class="nav">
        <li class="nav-item">
            <a class="nav-link" href="/"><button class="btn btn-outline-primary" id="homepage">Homepage</button></a>
        </li>
        <li class="nav-item">
            <!-- Conditionally render login or logout links -->
            {{#if logged_in}}
            <a class="nav-link" href="/profile">Profile</a>
            <a class="nav-link" href="/logout">
                <button class="btn btn-outline-dark" id="logout">Logout</button></a>
            {{else}}
            <a class="nav-link" href="/login"><button class="btn btn-outline-dark" id="login">Login</button></a>
            {{/if}}
        </li>
</footer>