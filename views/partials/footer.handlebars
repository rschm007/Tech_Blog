<footer class="d-flex justify-content-center p-2">
    <ul class="nav">
        <li class="nav-item">
            <a class="nav-link" href="/"><button class="btn btn-outline-primary btn-sm" id="homepage">Homepage</button></a>
        </li>
        <li class="nav-item">
            <!-- Conditionally render login or logout links -->
            {{#if logged_in}}
            <a class="nav-link" href="/logout">
                <button class="btn btn-outline-dark btn-sm" id="logout">Logout</button></a>
            {{else}}
            <a class="nav-link" href="/login"><button class="btn btn-outline-dark btn-sm" id="login">Login</button></a>
            {{/if}}
        </li>
</footer>