<nav class="navbar navbar-expand-lg navbar-light bg-light p-3">
    <span class="navbar-brand mb-0 h1">Technically a Tech Blog</span>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">

            <li class="nav-item active">
                <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
            </li>

            <!-- Conditionally render login or logout links -->
            {{#if logged_in}}
            <li class="nav-item">
                <a class="nav-link" href="/profile">Profile</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="/profile/create/">Create Post</a>
            </li>

            <li class="nav-item">
                <a href="/logout"><button class="btn btn-primary" id="logout">Logout</button></a>
            </li>

            {{else}}
            <li class="nav-item">
                <a href="/login"><button class="btn btn-primary" id="login">Login</button></a>
            </li>
            {{/if}}
        </ul>
    </div>
</nav>